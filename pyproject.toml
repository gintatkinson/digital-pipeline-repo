[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "digital-pipeline-repo"
version = "0.1.0"
description = "Digital Pipeline Repository"
requires-python = ">=3.10"
