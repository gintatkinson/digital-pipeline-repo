[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "deap-avionic-flight-safety"
version = "0.1.0"
description = "Civil Avionic Flight Safety & Safety-Critical Airborne System Platform for DEAP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
